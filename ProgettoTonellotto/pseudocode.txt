Pseudo-codice PageRank:

class Mapper:
	method Map
		int numPages = countPages(input)
		parseXML(value)
		for all externalLinks do
			calculateInitialPageRank()
			EMIT(pageName, < initialPageRank, <external link> >) // pageA 1 pageB , pageA 1 pageC ..
		
class Reducer:
	method Reduce
		